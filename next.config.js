/** @type {import('next').NextConfig} */
const nextConfig = {
  // Uncomment the lines below to enable static export for GitHub Pages deployment:
  // output: 'export',
  // basePath: '/Daksh-Lakhotiya',
};

module.exports = nextConfig;
